## Running the project

1. Install the dependencies:

```sh
pnpm install
```
<sub>`npm install` / `yarn install` also work</sub>

2. Start the development server:

```sh
pnpm run dev
```

## Building

To create a production version of your app:

```sh
pnpm run build
```

You can preview the production build with `pnpm run preview`.